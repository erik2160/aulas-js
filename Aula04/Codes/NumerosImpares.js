let contagem = 1; // inicializa a contagem

while (contagem <= 20) {
  // enquanto a contagem for menor do que 20
  if (contagem % 2 !== 0) {
    // se o número for ímpar
    console.log(`Número ímpar: ${contagem}`); // printa o numero
  }
  contagem++; // icrementa a contagem
}
