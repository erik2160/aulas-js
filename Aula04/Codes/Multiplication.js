const multiplicationNumber = 7;

for (let i = 1; i <= 10; i++) {
  // runs for numbers from 1 to 10
  console.log(`${multiplicationNumber} x ${i} = ${multiplicationNumber * i}`); // multiplies the multiplication number by 1 to 10
}
