let count = 1; // initializes the count

while (count <= 20) {
  // while the count is less than or equal to 20
  if (count % 2 !== 0) {
    // if the number is odd
    console.log(`Odd number: ${count}`); // prints the number
  }
  count++; // increments the count
}
